<UserControl x:Class="SpawnDev.ILGPU.WpfDemo.Pages.TestsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <!-- Action button style -->
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Padding" Value="18,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="6" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="🧪 Unit Tests" FontSize="22" FontWeight="Bold" Foreground="#E0E0FF"
                       VerticalAlignment="Center" Margin="0,0,20,0" />
            <ComboBox x:Name="BackendFilter" Width="160" FontSize="13" Margin="0,0,12,0"
                      SelectionChanged="OnBackendFilterChanged" VerticalAlignment="Center">
                <ComboBoxItem Content="CUDA" IsSelected="True" Tag="CUDA" />
                <ComboBoxItem Content="OpenCL" Tag="OpenCL" />
                <ComboBoxItem Content="All GPU Backends" Tag="All" />
            </ComboBox>
            <Button x:Name="RunAllBtn" Content="▶ Run All" Click="OnRunAll"
                    Background="#2B6B3B" Style="{StaticResource ActionBtn}" Margin="0,0,8,0" />
            <Button x:Name="CancelBtn" Content="⏹ Cancel" Click="OnCancel"
                    Background="#8B2B2B" Style="{StaticResource ActionBtn}" IsEnabled="False" />
        </StackPanel>

        <!-- Summary bar -->
        <Border Grid.Row="1" Background="#1E1E3A" CornerRadius="6" Padding="12,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Text="⏸ Ready" FontWeight="Bold"
                           Foreground="#A0A0D0" FontSize="14" Margin="0,0,20,0" />
                <TextBlock x:Name="SummaryText" Text="" Foreground="#8888B8" FontSize="13" />
            </StackPanel>
        </Border>

        <!-- Progress bar -->
        <ProgressBar x:Name="Progress" Grid.Row="2" Height="4" Margin="0,0,0,8"
                     Background="#2A2A4A" Foreground="#4A8B5B" Minimum="0" Maximum="100" Value="0" />

        <!-- Test results list -->
        <ListView x:Name="TestList" Grid.Row="3" Background="Transparent" BorderThickness="0"
                  Foreground="#C0C0E0" FontSize="13"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="#" Width="40" DisplayMemberBinding="{Binding Index}" />
                    <GridViewColumn Header="Backend" Width="80" DisplayMemberBinding="{Binding ClassName}" />
                    <GridViewColumn Header="Test" Width="260" DisplayMemberBinding="{Binding MethodName}" />
                    <GridViewColumn Header="Duration" Width="80" DisplayMemberBinding="{Binding DurationText}" />
                    <GridViewColumn Header="Result" Width="500">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ResultText}" Foreground="{Binding ResultColor}"
                                           TextWrapping="Wrap" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Footer with total duration -->
        <Border Grid.Row="4" Background="#1E1E3A" CornerRadius="6" Padding="12,6" Margin="0,8,0,0">
            <TextBlock x:Name="FooterText" Text="" Foreground="#8888B8" FontSize="12" />
        </Border>
    </Grid>
</UserControl>
